module(name = "demo")
bazel_dep(name = "rules_cc", version = "0.2.14")
bazel_dep(name = "fmt", version = "12.1.0")

single_version_override(
    module_name = "fmt",
    version = "12.1.0",
    patches = [
        ":fix.patch",
    ],
)
